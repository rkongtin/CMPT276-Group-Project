<!DOCTYPE html>
<html>
<head>
  <title>Workspace</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<!-- this is how the layout for the application works //-->
<body>
  
  <%= render 'layouts/header' %> <!-- place the header on top //-->
  <div class="container">
    <% flash.each do |message_type, message| %><!-- any errors? display them //-->
    <div class="alert alert-<%= message_type %>"><%= message %></div>
    <% end %>
    <%= yield %> <!-- insert whatever page's elements we're on here (such as the view users/ID/home //-->
    <%= debug(params) if Rails.env.development? %> <!-- place a debug area here if we're developing //-->
  </div>

</body>
</html>
